import SwiftUI

public extension Color {
    static let rmBackground = Color(.systemBackground)
    static let rmSecondary = Color(.secondarySystemBackground)
    static let rmAccent = Color.accentColor
}
